// TALK: frist we call kaboom() to initialize the kaboom context
// TALK: we pass in a 'global' flag to import all kaboom functions to global namespace for easiness!
// TALK: this will give us a nice black 640x480 canvas!

kaboom({
	global: true,
});
