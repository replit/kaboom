// TALK: We can use the function `addSprite()` to add a sprite to the screen
// TALK: We pass in the sprite name `"mark"`, which is a default sprite available in kaboom
// TALK: Now now we have a me on screen!
// TALK: and yep you just need one line to get stuff on screen.
// TALK: Next let's add some properties to me

kaboom({
	global: true,
});

addSprite("mark");
